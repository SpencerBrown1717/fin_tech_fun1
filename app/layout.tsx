import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'FinGuard AI | Enterprise Compliance Automation',
  description: 'Next-generation agentic AI platform for financial compliance monitoring and enforcement with specialized agents, enterprise-grade security, and complete auditability.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
